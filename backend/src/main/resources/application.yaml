spring:
  application:
    name: sisInf
  datasource:
    password: Alex2001
    username: postgres
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/mydb?currentSchema=sisinf
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false

server:
  port: 8180

